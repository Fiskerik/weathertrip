{
  "name": "weathertrip",
  "version": "0.1.0",
  "private": true,
  "description": "Weathertrip - plan your next trip based on the weather.",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm --workspace @weathertrip/api run dev",
    "dev:web": "npm --workspace @weathertrip/web run dev",
    "dev:mobile": "npm --workspace @weathertrip/mobile run start",
    "build": "npm --workspace @weathertrip/shared run build && npm --workspace @weathertrip/api run build && npm --workspace @weathertrip/web run build",
    "check": "npm --workspace @weathertrip/shared run typecheck && npm --workspace @weathertrip/api run typecheck && npm --workspace @weathertrip/web run typecheck && npm --workspace @weathertrip/mobile run typecheck"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "pngjs": "^7.0.0",
    "typescript": "^5.5.4"
  }
}
